fastapi==0.128.0
uvicorn[standard]==0.40.0
joblib==1.5.1
scikit-learn==1.6.1

# Fixações de dependências transitivas motivadas por CVE (mesmo espírito das
# sobrescritas do BOM no pom.xml do backend). Sem isso, requirements.txt não
# fixa essas versões, e ferramentas de scan sem lockfile (OSV-Scanner) assumem
# o pior caso permitido pelas faixas declaradas pelos pacotes diretos — o pip
# já resolve para uma versão segura hoje (httpcore exige h11>=0.16), mas fixar
# explicitamente remove a ambiguidade e faz o scanner parar de acusar.
anyio>=4.14.2   # GHSA-82r6-8w77-94w6, CVSS 9.3 (Critical)
h11>=0.16.0     # PYSEC-2026-348, CVSS 9.1 (Critical)
